# Bitegit App Icon

Place your logo image here to generate Android app icons.

Required file names:

- `resources/icon.png` (recommended square PNG, 1024x1024)
- `resources/splash.png` (optional, same size)

Then run:

```bash
npm run mobile:icons
npm run mobile:sync
```

This will generate launcher icons inside the Android project.
